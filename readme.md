## Version

Bump `STR.version` in `strings.js` on every code change. Use semantic versioning (PATCH for fixes, MINOR for features, MAJOR for breaking changes). See `.cursor/rules/project-instructions.mdc` for details.

## Tests

Open `tests/run.html` in a browser to run all tests.

**Suites:** effect-registry, selection-persistence, revert-death, revert-vote-after-reopen, revert-flow-actions, matador-disabled, sixth-sense, status-check-eliminated, constantine-once-per-game, godfather, i18n-farsi, end-card-action, saul-buy-ui, scroll-persistence, player-selection-cards, night-step-role-filter, capo_test_instructions, classic, bazras, zodiac, takavar.

## TODO

- **Guns manager (Zodiac):** Use two target dropdowns (e.g. "Player1's Target", "Player2's Target") instead of shooter + target selection.
